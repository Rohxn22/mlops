# Runtime dependencies — only what's needed to serve the FastAPI prediction API
# MLflow is intentionally excluded (model is baked into the image as a .pkl file)

numpy>=1.26
pandas>=2.0
scikit-learn>=1.4
xgboost>=2.0
joblib

fastapi>=0.103
pydantic>=2.4
uvicorn>=0.23
python-multipart

boto3
prometheus_fastapi_instrumentator